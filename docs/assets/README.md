# Screenshots

Drop UI screenshots here and reference them from the top-level `README.md`.

Recommended captures (run `docker compose up --build`, open http://localhost:3000):

- `dashboard.png` — the analysis dashboard: error-group rail + AI insight panel + confidence.
- `chat.png` — chat-with-logs mid-stream, showing cited line ranges.
- `investigation.png` — the multi-agent investigation trace + timeline.

Tip: a short animated GIF (e.g. via ScreenToGif / LICEcap) of paste → analysis → chat is the single
highest-impact thing you can add to the README.
